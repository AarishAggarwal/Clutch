# Archived SQLite migrations

These folders were applied when the app used **SQLite** (`file:./dev.db`). The project now uses **PostgreSQL** (e.g. Vercel Postgres / Neon). The active baseline is:

`prisma/migrations/20260201120000_postgres_baseline/`

Keep this archive only for historical reference; Prisma does not load it.
