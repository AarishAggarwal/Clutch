# Copy to `.env.local`. Next.js loads this over `.env`.

# =============================================================================
# PostgreSQL for Prisma (Vercel Postgres, Neon, Railway, etc. — not Supabase)
# =============================================================================
#
# Vercel (recommended for this repo):
# 1. Deploy once or create a project.
# 2. Open the project → Storage → Create Database → Postgres (Vercel Postgres / Neon).
# 3. In the database panel, open ".env.local" tab — Vercel shows variables like:
#    - POSTGRES_PRISMA_URL   → use this as DATABASE_URL (pooled; good for serverless)
#    - POSTGRES_URL_NON_POOLING → use this as DIRECT_URL (migrations)
#    (Exact names are shown in the UI; copy the Prisma + non-pooling pair.)
#
# 4. In Vercel → Project → Settings → Environment Variables, add:
#    DATABASE_URL    = (pooled / Prisma URL)
#    DIRECT_URL      = (non-pooling URL)
#    Plus any OPENAI_API_KEY etc.
#
# 5. Locally: paste the same two URLs into .env.local AND .env so Prisma CLI works.

DATABASE_URL=""
DIRECT_URL=""

OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
OPENAI_EVAL_MODEL="gpt-4o-mini"
ANTHROPIC_EVAL_MODEL="claude-3-5-sonnet-latest"
ANTHROPIC_BASE_URL=""

COLLEGE_SCORECARD_API_KEY=""

NEXT_PUBLIC_APPS_OPEN_AT="2026-08-01T00:00:00-04:00"
